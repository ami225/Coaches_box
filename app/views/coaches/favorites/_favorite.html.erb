<!-- いいねボタン -->
<% if @coach.favorited_by?(current_user) %>
  <%= link_to coaches_coach_favorites_path(@coach), method: :delete, remote: true do %>
  ♥<%= @coach.favorites.count %> Good Coach!!
  <% end %>
<% else %>
  <%= link_to coaches_coach_favorites_path(@coach), method: :post, remote: true do %>
  ♡<%= @coach.favorites.count %> Good Coach?
  <% end %>
<% end %>